# 3 Card Blackjack

- 3 card Blackjack written in Java with javafx
- Full server client communication
- Requires Maven
- Requires Java(11 or higher)
- Requires Java FX
- run with `mvn compile` and `mvn exec:java`
